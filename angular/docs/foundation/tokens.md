# Foundation — Tokens

> Placeholder. Tokens are already ported to `angular/packages/weoc-ui-css/tokens/` (Wave 1 Task 3) —
> this page needs to be written up from the real files, not re-derived.

## To document

- [ ] Color scale (`--color-*`, `--color-on-accent`)
- [ ] Spacing scale (`--space-*`)
- [ ] Radius scale (`--radius-*`)
- [ ] Typography tokens (`--font-*`)
- [ ] Shadow scale (`--shadow-*`)
- [ ] Theme switching (`data-theme` attribute, light/dark) — and how it differs from PrimeNG's
      `prefers-color-scheme`-driven auto theme (see `../primeng-bridge/select-datepicker.md`)
