# Loader docs reorg report

- Status: DONE — pure docs-move, as expected. No NEEDS_CONTEXT case hit.
- Confirmed `build/src/interaction/loader.js` and `CSS/weoc-ui/weoc-loader.css` already existed, were fully wired into `build/src/index.js`, and were present/functional in the built `JS/weoc-ui.js` bundle (verified `WUI.busy`/`WUI.buttonBusy` present in the bundle, and cross-referenced `build/test/loader.test.js` for exact semantics). No JS/CSS engineering was done or needed.
- New page: `docs/docs/loader.html` (+ `docs/i18n/loader.js`, 25 EN/AR string pairs). Documents `WUI.busy()` full-page, `WUI.busy(el)` scoped, `WUI.buttonBusy()`, declarative `data-wui-loading` (form + button), the `LoaderPleaseWait` i18n key, and the `.spin` icon utility (moved wholesale off Feedback, since it's loading-related, not feedback-related).
- `docs/docs/feedback.html`: removed the old Loader/`.spin` section (was mislabeled — it only ever documented `.spin`, never `WUI.busy`/`buttonBusy`/`data-wui-loading`, which had no docs anywhere before this change); added a cross-link to the new Loader page in the hero-sub. All other Feedback content (callout, banner, toast, snackbar, alarm, skeleton, empty-state) untouched.
- NAV group used: "Feedback" (docs-shell.js), positioned between Feedback and Progress. Also added `loader` to the `I18N_PAGES` site-wide loader list.
- Commit: NOT completed — blocked by the shared pre-commit hook (see Concerns). All 6 files are staged and ready; committing needs either the kpi-recipes.html gap fixed upstream or an explicit human decision to bypass the hook once.
- Concerns:
  1. This worktree's branch predates the search-index feature (`tools/build-search-index.js` isn't in this branch's history at all, though `bao-weoc-ui-updates` upstream has it) — the pre-commit hook (via a shared `core.hooksPath`) referenced a script this worktree didn't have. Worked around by temporarily copying `tools/` from the main checkout (not committed, not part of this diff) so the hook could actually run and be evaluated honestly rather than skipped.
  2. With `tools/` present, the hook runs `node tools/build-search-index.js`, which scans and disk-patches missing `id=` attributes into every docs page site-wide (not just changed ones) as an unconditional side effect, THEN checks for unresolved i18n references across the whole site and aborts the commit if any exist — before staging or reverting its own patches. It found one pre-existing, unrelated gap: `kpi-recipes.html` is missing an AR entry for `docs_kpi_11b`. That file is explicitly off-limits (another agent is actively editing it), so I could not fix it myself, and did not use `--no-verify` (against my instructions to never skip hooks without explicit user request). Reverted the incidental disk-patching the failed run left behind on all untouched pages (git checkout, not `git add`) both times this happened, so the worktree carries only my intended diff. `tools/` was removed again afterward since it is not part of my task's file set.
  3. Net effect: the commit is genuinely blocked by another agent's unfinished work, not by anything in the Loader/Feedback change itself. Recommend either waiting for `kpi-recipes.html`'s AR gap to be closed, or having the integrator commit this branch with the hook run once that gap is fixed.
  4. Minor follow-up (not done, out of assigned scope): `docs/index.html` homepage feature-grid still only cards Feedback + Progress under its "Feedback" section — could add a Loader card later for discoverability. Left untouched to avoid conflicting with other parallel agents.
