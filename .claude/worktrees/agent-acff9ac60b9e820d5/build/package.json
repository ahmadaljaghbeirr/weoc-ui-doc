{
  "name": "weoc-ui-build",
  "version": "0.1.0",
  "description": "weoc-ui library build: modular ES source in src/ -> single ES5 IIFE bundle at JS/weoc-ui.js. Boards reference the built artifact only.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "build:min": "rollup -c --environment MINIFY",
    "test": "node --test"
  },
  "devDependencies": {
    "@babel/core": "^7.29.7",
    "@babel/preset-env": "^7.29.7",
    "@rollup/plugin-babel": "^7.1.0",
    "@rollup/plugin-terser": "^1.0.0",
    "jsdom": "^29.1.1",
    "rollup": "^4.62.2"
  }
}
