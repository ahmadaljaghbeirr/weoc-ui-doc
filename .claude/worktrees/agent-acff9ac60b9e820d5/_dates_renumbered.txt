{ lang:'en', id:'docs_forms_47', value:'Dates' }, { lang:'ar', id:'docs_forms_47', value:'التواريخ' },
{ lang:'en', id:'docs_forms_48', value:'Date & Time' }, { lang:'ar', id:'docs_forms_48', value:'التاريخ والوقت' },
{ lang:'en', id:'docs_forms_49', value:'Date, time, datetime, and range pickers powered by <code>flatpickr-factory.js</code>. Add <code>data-fp-preset</code> to any <code>&lt;input&gt;</code> and the factory auto-inits it, styling it with <code>flatpickr-input</code> and the <code>flat-date</code> / <code>flat-time</code> / <code>flat-range-*</code> skins. Vendor CSS/JS are loaded globally by the docs shell.' }, { lang:'ar', id:'docs_forms_49', value:'منتقيات للتاريخ والوقت والتاريخ والوقت والنطاق مدعومة بواسطة <code>flatpickr-factory.js</code>. أضف <code>data-fp-preset</code> إلى أي <code>&lt;input&gt;</code> وسيقوم المصنع بتهيئته تلقائيًا، مع تنسيقه باستخدام <code>flatpickr-input</code> وأصناف <code>flat-date</code> / <code>flat-time</code> / <code>flat-range-*</code>. يتم تحميل ملفات CSS/JS الخاصة بالمورّد بشكل عام بواسطة غلاف التوثيق.' },
{ lang:'en', id:'docs_forms_50', value:'<span class="material-symbols-outlined">bolt</span>How the factory binds' }, { lang:'ar', id:'docs_forms_50', value:'<span class="material-symbols-outlined">bolt</span>كيفية ربط المصنع' },
{ lang:'en', id:'docs_forms_51', value:'The factory exposes the global <code>window.FlatpickrFactory</code> with <code>init(scope)</code>, <code>PRESETS</code>, and <code>FMT</code>. On load it calls <code>FlatpickrFactory.init()</code>, which runs <code>querySelectorAll(\'[data-fp-preset]\')</code> over the scope and initializes every matching input (already-initialized inputs carry <code>_flatpickr</code> and are skipped). Every input dispatches a bubbling <code>fp:change</code> CustomEvent (<code>e.detail.dateStr</code>) on selection.' }, { lang:'ar', id:'docs_forms_51', value:'يوفّر المصنع الكائن العام <code>window.FlatpickrFactory</code> مع <code>init(scope)</code> و<code>PRESETS</code> و<code>FMT</code>. عند التحميل يستدعي <code>FlatpickrFactory.init()</code>، الذي يشغّل <code>querySelectorAll(\'[data-fp-preset]\')</code> على النطاق ويهيّئ كل إدخال مطابق (الإدخالات المهيّأة مسبقًا تحمل <code>_flatpickr</code> ويتم تخطّيها). يرسل كل إدخال حدث <code>fp:change</code> من نوع CustomEvent (<code>e.detail.dateStr</code>) عند الاختيار.' },
{ lang:'en', id:'docs_forms_52', value:'Re-init after SPA navigation' }, { lang:'ar', id:'docs_forms_52', value:'إعادة التهيئة بعد التنقل في تطبيق الصفحة الواحدة' },
{ lang:'en', id:'docs_forms_53', value:'These demos are initialized by <code>PAGE_INIT.dates</code> in <code>docs-shell.js</code> (which calls <code>FlatpickrFactory.init()</code>), so they re-bind after every Barba/SPA swap. The classes <code>flat-date</code> / <code>flat-time</code> / <code>flat-range-start</code> / <code>flat-range-end</code> are cosmetic only; binding is driven entirely by <code>data-fp-preset</code>.' }, { lang:'ar', id:'docs_forms_53', value:'تتم تهيئة هذه العروض التوضيحية بواسطة <code>PAGE_INIT.dates</code> في <code>docs-shell.js</code> (الذي يستدعي <code>FlatpickrFactory.init()</code>)، لذا تُعاد ربطها بعد كل تبديل في Barba/تطبيق الصفحة الواحدة. الأصناف <code>flat-date</code> / <code>flat-time</code> / <code>flat-range-start</code> / <code>flat-range-end</code> تجميلية فقط؛ يُدار الربط بالكامل عبر <code>data-fp-preset</code>.' },
{ lang:'en', id:'docs_forms_54', value:'<span class="material-symbols-outlined">calendar_today</span>Date only' }, { lang:'ar', id:'docs_forms_54', value:'<span class="material-symbols-outlined">calendar_today</span>التاريخ فقط' },
{ lang:'en', id:'docs_forms_55', value:'Calendar picker, no time. <code>date</code> renders ISO <code>Y-m-d</code>; <code>date-dmy</code> renders <code>d/m/Y</code>.' }, { lang:'ar', id:'docs_forms_55', value:'منتقي تقويم، بدون وقت. <code>date</code> يعرض بصيغة ISO <code>Y-m-d</code>؛ <code>date-dmy</code> يعرض <code>d/m/Y</code>.' },
{ lang:'en', id:'docs_forms_56', value:'date / date-dmy' }, { lang:'ar', id:'docs_forms_56', value:'date / date-dmy' },
{ lang:'en', id:'docs_forms_57', value:'<span class="material-symbols-outlined">schedule</span>Time only' }, { lang:'ar', id:'docs_forms_57', value:'<span class="material-symbols-outlined">schedule</span>الوقت فقط' },
{ lang:'en', id:'docs_forms_58', value:'<code>noCalendar</code> presets — the time spinner with no date. <code>time</code> is 24hr (<code>H:i</code>); <code>time-12</code> is 12hr with AM/PM (<code>h:i K</code>).' }, { lang:'ar', id:'docs_forms_58', value:'إعدادات <code>noCalendar</code>: مؤشر الوقت الدوّار بدون تاريخ. <code>time</code> بنظام 24 ساعة (<code>H:i</code>)؛ <code>time-12</code> بنظام 12 ساعة مع AM/PM (<code>h:i K</code>).' },
{ lang:'en', id:'docs_forms_59', value:'time / time-12' }, { lang:'ar', id:'docs_forms_59', value:'time / time-12' },
{ lang:'en', id:'docs_forms_60', value:'<span class="material-symbols-outlined">event</span>Date + time' }, { lang:'ar', id:'docs_forms_60', value:'<span class="material-symbols-outlined">event</span>التاريخ والوقت' },
{ lang:'en', id:'docs_forms_61', value:'<code>enableTime</code> presets combining calendar and clock. Format and 12/24hr vary per preset; <code>datetime-seconds</code> adds a seconds spinner.' }, { lang:'ar', id:'docs_forms_61', value:'إعدادات <code>enableTime</code> تجمع بين التقويم والساعة. تختلف الصيغة ونظام 12/24 ساعة حسب كل إعداد؛ <code>datetime-seconds</code> يضيف مؤشر ثوانٍ دوّار.' },
{ lang:'en', id:'docs_forms_62', value:'datetime / datetime-dmy / datetime-12 / datetime-seconds' }, { lang:'ar', id:'docs_forms_62', value:'datetime / datetime-dmy / datetime-12 / datetime-seconds' },
{ lang:'en', id:'docs_forms_63', value:'<span class="material-symbols-outlined">history_toggle_off</span>Future-only &amp; past-only' }, { lang:'ar', id:'docs_forms_63', value:'<span class="material-symbols-outlined">history_toggle_off</span>المستقبل فقط والماضي فقط' },
{ lang:'en', id:'docs_forms_64', value:'Bounded presets that clamp the selectable range against <em>now</em>. The min/max is refreshed in an <code>onOpen</code> hook so the boundary stays accurate across a long-lived page. <code>past-datetime</code> also adds 12hr time + seconds.' }, { lang:'ar', id:'docs_forms_64', value:'إعدادات محدودة تقيّد النطاق القابل للاختيار مقابل <em>الآن</em>. يتم تحديث الحد الأدنى/الأقصى في خطاف <code>onOpen</code> حتى يبقى الحدّ دقيقًا عبر صفحة طويلة الأمد. <code>past-datetime</code> يضيف أيضًا وقتًا بنظام 12 ساعة مع الثواني.' },
{ lang:'en', id:'docs_forms_65', value:'future-date / past-date' }, { lang:'ar', id:'docs_forms_65', value:'future-date / past-date' },
{ lang:'en', id:'docs_forms_66', value:'future-datetime / past-datetime' }, { lang:'ar', id:'docs_forms_66', value:'future-datetime / past-datetime' },
{ lang:'en', id:'docs_forms_67', value:'<span class="material-symbols-outlined">date_range</span>Date range (paired)' }, { lang:'ar', id:'docs_forms_67', value:'<span class="material-symbols-outlined">date_range</span>نطاق التاريخ (مقترن)' },
{ lang:'en', id:'docs_forms_68', value:'Two inputs that constrain each other: both share a <code>data-fp-range-group</code>, with <code>data-fp-range-role="start"</code> on one and <code>"end"</code> on the other. The start\'s selection becomes the end\'s minimum and vice-versa. Wrap the pair in <code>wui-date-range-wrap</code> for the tight flex row.' }, { lang:'ar', id:'docs_forms_68', value:'إدخالان يقيّد كل منهما الآخر: يتشاركان <code>data-fp-range-group</code>، مع <code>data-fp-range-role="start"</code> على أحدهما و<code>"end"</code> على الآخر. يصبح اختيار البداية الحدّ الأدنى للنهاية والعكس صحيح. لُفّ الزوج داخل <code>wui-date-range-wrap</code> للحصول على صف flex متضام.' },
{ lang:'en', id:'docs_forms_69', value:'range-start + range-end' }, { lang:'ar', id:'docs_forms_69', value:'range-start + range-end' },
{ lang:'en', id:'docs_forms_70', value:'<span class="material-symbols-outlined">calendar_month</span>Inline calendar' }, { lang:'ar', id:'docs_forms_70', value:'<span class="material-symbols-outlined">calendar_month</span>تقويم مضمّن' },
{ lang:'en', id:'docs_forms_71', value:'<code>data-fp-inline="true"</code> embeds the calendar permanently in place instead of opening a popup. Works with any preset; useful for date panels and dashboard filter widgets.' }, { lang:'ar', id:'docs_forms_71', value:'<code>data-fp-inline="true"</code> يضمّن التقويم بشكل دائم في مكانه بدلاً من فتح نافذة منبثقة. يعمل مع أي إعداد؛ مفيد للوحات التاريخ وأدوات تصفية لوحات المعلومات.' },
{ lang:'en', id:'docs_forms_72', value:'date · inline' }, { lang:'ar', id:'docs_forms_72', value:'date · inline' },
{ lang:'en', id:'docs_forms_73', value:'<span class="material-symbols-outlined">tune</span>Per-input overrides' }, { lang:'ar', id:'docs_forms_73', value:'<span class="material-symbols-outlined">tune</span>تجاوزات لكل إدخال' },
{ lang:'en', id:'docs_forms_74', value:'Any preset can be tuned inline with <code>data-fp-*</code> overrides: <code>data-fp-format</code> (a <code>Y-m-d</code> string or an alias like <code>dmy-hi</code>), <code>data-fp-min-date</code> / <code>data-fp-max-date</code>, <code>data-fp-enable-time</code>, <code>data-fp-24hr</code>, <code>data-fp-seconds</code>, <code>data-fp-allow-input</code>, <code>data-fp-week-numbers</code>, <code>data-fp-mode</code> (<code>single</code>/<code>multiple</code>/<code>range</code>), <code>data-fp-position</code>, <code>data-fp-default-date</code>, and <code>data-fp-on-change</code> (name of a global handler).' }, { lang:'ar', id:'docs_forms_74', value:'يمكن ضبط أي إعداد بشكل مباشر باستخدام تجاوزات <code>data-fp-*</code>: <code>data-fp-format</code> (سلسلة <code>Y-m-d</code> أو اسم مستعار مثل <code>dmy-hi</code>)، <code>data-fp-min-date</code> / <code>data-fp-max-date</code>، <code>data-fp-enable-time</code>، <code>data-fp-24hr</code>، <code>data-fp-seconds</code>، <code>data-fp-allow-input</code>، <code>data-fp-week-numbers</code>، <code>data-fp-mode</code> (<code>single</code>/<code>multiple</code>/<code>range</code>)، <code>data-fp-position</code>، <code>data-fp-default-date</code>، و<code>data-fp-on-change</code> (اسم معالج عام).' },
{ lang:'en', id:'docs_forms_75', value:'Override examples' }, { lang:'ar', id:'docs_forms_75', value:'أمثلة على التجاوزات' },
{ lang:'en', id:'docs_forms_76', value:'<span class="material-symbols-outlined">lock</span>Readonly display' }, { lang:'ar', id:'docs_forms_76', value:'<span class="material-symbols-outlined">lock</span>عرض للقراءة فقط' },
{ lang:'en', id:'docs_forms_77', value:'Flatpickr always sets <code>[readonly]</code> internally. For a true display-only field, add <code>.is-readonly</code> to the input and disable opening with <code>fp.set(\'clickOpens\', false)</code>. The dashed border and muted surface match the form-control readonly language.' }, { lang:'ar', id:'docs_forms_77', value:'يضبط Flatpickr دائمًا <code>[readonly]</code> داخليًا. للحصول على حقل للعرض فقط بالكامل، أضف <code>.is-readonly</code> إلى الإدخال وعطّل الفتح باستخدام <code>fp.set(\'clickOpens\', false)</code>. يطابق الحدّ المتقطّع والسطح الباهت لغة القراءة فقط الخاصة بعنصر التحكم في النموذج.' },
{ lang:'en', id:'docs_forms_78', value:'Editable vs readonly' }, { lang:'ar', id:'docs_forms_78', value:'قابل للتحرير مقابل للقراءة فقط' },