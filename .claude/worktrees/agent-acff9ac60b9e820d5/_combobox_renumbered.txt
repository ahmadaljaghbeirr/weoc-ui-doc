{ lang:'en', id:'docs_forms_79', value:'Combobox' }, { lang:'ar', id:'docs_forms_79', value:'قائمة الاختيار' },
{ lang:'en', id:'docs_forms_80', value:'Select / Combobox' }, { lang:'ar', id:'docs_forms_80', value:'قائمة الاختيار / Combobox' },
{ lang:'en', id:'docs_forms_81', value:'The <code>tom-select-factory.js</code> combobox. Add <code>class="tomselect"</code> to any <code>&lt;select&gt;</code>, drive every behavior through <code>data-*</code> attributes, and call <code>TomSelectFactory.init()</code>. The factory injects a blank placeholder option, resyncs dependent cascades, mounts pinned dropdown header/footer slots, and follows the agency theme. Every capability is demoed below with the exact markup and the exact init call.' }, { lang:'ar', id:'docs_forms_81', value:'قائمة الاختيار في <code>tom-select-factory.js</code>. أضف <code>class="tomselect"</code> إلى أي <code>&lt;select&gt;</code>، وتحكم في كل سلوك من خلال سمات <code>data-*</code>، ثم استدعِ <code>TomSelectFactory.init()</code>. يقوم المصنع بإدراج خيار عنصر نائب فارغ، ويعيد مزامنة القوائم المتسلسلة التابعة، ويثبّت فتحات رأس/تذييل القائمة المنسدلة المثبتة، ويتبع سمة الوكالة. يتم عرض كل إمكانية أدناه مع الترميز الدقيق واستدعاء التهيئة الدقيق.' },
{ lang:'en', id:'docs_forms_82', value:'<span class="material-symbols-outlined">bolt</span>How it initializes' }, { lang:'ar', id:'docs_forms_82', value:'<span class="material-symbols-outlined">bolt</span>كيفية التهيئة' },
{ lang:'en', id:'docs_forms_83', value:'The factory binds every <code>&lt;select class="tomselect"&gt;</code> in scope. Call <code>TomSelectFactory.init()</code> once after the DOM is ready. It also starts a <code>MutationObserver</code>, so late-injected selects (WebEOC repeat regions, modals) initialize automatically. Retrieve a live instance with <code>TomSelectFactory.get(\'idOrName\')</code>; drive it with <code>refresh</code>, <code>destroy</code>, <code>enable</code>, <code>disable</code>.' }, { lang:'ar', id:'docs_forms_83', value:'يربط المصنع كل <code>&lt;select class="tomselect"&gt;</code> ضمن النطاق. استدعِ <code>TomSelectFactory.init()</code> مرة واحدة بعد جاهزية الـ DOM. كما يبدأ <code>MutationObserver</code>، لذا تتم تهيئة قوائم الاختيار المُدرَجة لاحقًا (مناطق التكرار والنوافذ المنبثقة في WebEOC) تلقائيًا. استرجع نسخة نشطة باستخدام <code>TomSelectFactory.get(\'idOrName\')</code>، وتحكم فيها عبر <code>refresh</code> و<code>destroy</code> و<code>enable</code> و<code>disable</code>.' },
{ lang:'en', id:'docs_forms_84', value:'Load order' }, { lang:'ar', id:'docs_forms_84', value:'ترتيب التحميل' },
{ lang:'en', id:'docs_forms_85', value:'<code>tom-select.min.css</code> → <code>tom-select-agency.css</code> in <code>&lt;head&gt;</code>, then <code>tom-select.complete.min.js</code> → <code>tom-select-factory.js</code>. On this docs site the vendor CSS/JS is loaded site-wide by the shell; you only call <code>TomSelectFactory.init()</code>.' }, { lang:'ar', id:'docs_forms_85', value:'<code>tom-select.min.css</code> → <code>tom-select-agency.css</code> في <code>&lt;head&gt;</code>، ثم <code>tom-select.complete.min.js</code> → <code>tom-select-factory.js</code>. في موقع التوثيق هذا، يتم تحميل ملفات CSS/JS الخاصة بالمورّد على مستوى الموقع بالكامل عبر الهيكل؛ ما عليك سوى استدعاء <code>TomSelectFactory.init()</code>.' },
{ lang:'en', id:'docs_forms_86', value:'Instance key' }, { lang:'ar', id:'docs_forms_86', value:'مفتاح النسخة' },
{ lang:'en', id:'docs_forms_87', value:'The key is the select\'s <code>id</code>, or its <code>name</code> if there is no id (an auto <code>data-ts-key</code> is stamped only when both are absent). Use that key with <code>TomSelectFactory.get()</code> / <code>enable()</code> / <code>disable()</code> / <code>destroy()</code> / <code>refresh()</code>.' }, { lang:'ar', id:'docs_forms_87', value:'المفتاح هو <code>id</code> الخاص بقائمة الاختيار، أو <code>name</code> الخاص بها إذا لم يكن هناك id (تُختم سمة <code>data-ts-key</code> تلقائيًا فقط عند غياب كليهما). استخدم هذا المفتاح مع <code>TomSelectFactory.get()</code> / <code>enable()</code> / <code>disable()</code> / <code>destroy()</code> / <code>refresh()</code>.' },
{ lang:'en', id:'docs_forms_88', value:'<span class="material-symbols-outlined">arrow_drop_down_circle</span>Single select' }, { lang:'ar', id:'docs_forms_88', value:'<span class="material-symbols-outlined">arrow_drop_down_circle</span>اختيار مفرد' },
{ lang:'en', id:'docs_forms_89', value:'One item, with a built-in dropdown search field (<code>dropdown_input</code> plugin). The factory injects a blank placeholder option, so a fresh record shows the placeholder rather than auto-selecting the first real option. <code>data-placeholder</code> sets the placeholder text.' }, { lang:'ar', id:'docs_forms_89', value:'عنصر واحد، مع حقل بحث مدمج ضمن القائمة المنسدلة (الإضافة <code>dropdown_input</code>). يقوم المصنع بإدراج خيار عنصر نائب فارغ، لذا يعرض السجل الجديد العنصر النائب بدلًا من اختيار أول خيار حقيقي تلقائيًا. تحدد <code>data-placeholder</code> نص العنصر النائب.' },
{ lang:'en', id:'docs_forms_90', value:'Basic single (required — placeholder only)' }, { lang:'ar', id:'docs_forms_90', value:'اختيار مفرد أساسي (مطلوب: العنصر النائب فقط)' },
{ lang:'en', id:'docs_forms_91', value:'Without <code>data-allow-empty</code>, the placeholder shows on load but the user cannot re-select blank once a value is chosen. This is the correct pattern for required fields.' }, { lang:'ar', id:'docs_forms_91', value:'بدون <code>data-allow-empty</code>، يظهر العنصر النائب عند التحميل لكن لا يمكن للمستخدم إعادة اختيار القيمة الفارغة بعد اختيار قيمة. هذا هو النمط الصحيح للحقول المطلوبة.' },
{ lang:'en', id:'docs_forms_92', value:'Clearable single (data-allow-empty)' }, { lang:'ar', id:'docs_forms_92', value:'اختيار مفرد قابل للمسح (data-allow-empty)' },
{ lang:'en', id:'docs_forms_93', value:'<code>data-allow-empty="true"</code> keeps the empty option selectable in the dropdown, so the user can actively re-clear the field. The clear-all "×" also appears once a value is chosen.' }, { lang:'ar', id:'docs_forms_93', value:'تُبقي <code>data-allow-empty="true"</code> الخيار الفارغ قابلًا للاختيار في القائمة المنسدلة، بحيث يمكن للمستخدم مسح الحقل من جديد بشكل فعّال. كما يظهر زر مسح الكل "×" بمجرد اختيار قيمة.' },
{ lang:'en', id:'docs_forms_94', value:'Preselected value (edit mode)' }, { lang:'ar', id:'docs_forms_94', value:'قيمة محددة مسبقًا (وضع التحرير)' },
{ lang:'en', id:'docs_forms_95', value:'An <code>&lt;option selected&gt;</code> carrying the native <code>selected</code> attribute is honored — the factory only force-clears when no option has an explicit <code>selected</code> attribute (a browser default), so saved records repopulate correctly.' }, { lang:'ar', id:'docs_forms_95', value:'يُحترَم <code>&lt;option selected&gt;</code> الذي يحمل سمة <code>selected</code> الأصلية، فالمصنع يفرض المسح فقط عندما لا يحمل أي خيار سمة <code>selected</code> صريحة (وهو سلوك افتراضي للمتصفح)، بحيث تُعاد تعبئة السجلات المحفوظة بشكل صحيح.' },
{ lang:'en', id:'docs_forms_96', value:'<span class="material-symbols-outlined">checklist</span>Multi-select' }, { lang:'ar', id:'docs_forms_96', value:'<span class="material-symbols-outlined">checklist</span>اختيار متعدد' },
{ lang:'en', id:'docs_forms_97', value:'Add <code>multiple</code> (or <code>data-multi="true"</code>). Selections render as removable chips (the <code>remove_button</code> plugin adds a per-chip "×"), and an inline search field is always visible. The right-rail plus icon replaces the chevron. In a WebEOC edit view, restore comma-separated values with <code>repopulateMultiSelect(\'FieldName\', xslValue)</code> — WebEOC does not restore multi-select natively.' }, { lang:'ar', id:'docs_forms_97', value:'أضف <code>multiple</code> (أو <code>data-multi="true"</code>). تُعرض الاختيارات كوسوم قابلة للإزالة (تضيف الإضافة <code>remove_button</code> زر "×" لكل وسم)، ويظل حقل بحث مضمّن مرئيًا دائمًا. تحل أيقونة الزائد في الحافة اليمنى محل السهم. في عرض تحرير WebEOC، استعِد القيم المفصولة بفواصل باستخدام <code>repopulateMultiSelect(\'FieldName\', xslValue)</code>، فإن WebEOC لا يستعيد الاختيار المتعدد بشكل أصلي.' },
{ lang:'en', id:'docs_forms_98', value:'Multiple (chips + remove)' }, { lang:'ar', id:'docs_forms_98', value:'متعدد (وسوم + إزالة)' },
{ lang:'en', id:'docs_forms_99', value:'data-multi="true" (attribute form)' }, { lang:'ar', id:'docs_forms_99', value:'data-multi="true" (صيغة السمة)' },
{ lang:'en', id:'docs_forms_100', value:'<code>data-multi="true"</code> is equivalent to the native <code>multiple</code> attribute. Useful when the markup is generated by a system that will not emit a bare boolean attribute.' }, { lang:'ar', id:'docs_forms_100', value:'تعادل <code>data-multi="true"</code> سمة <code>multiple</code> الأصلية. مفيدة عندما يُنشئ الترميز نظامٌ لا يُصدر سمة منطقية مجردة.' },
{ lang:'en', id:'docs_forms_101', value:'Multi with preselected chips (edit mode)' }, { lang:'ar', id:'docs_forms_101', value:'اختيار متعدد بوسوم محددة مسبقًا (وضع التحرير)' },
{ lang:'en', id:'docs_forms_102', value:'Options carrying <code>selected</code> render as chips on load. For WebEOC, the page-level <code>repopulateMultiSelect</code> helper splits the saved comma-separated string, sets the native value, then calls <code>.sync()</code>.' }, { lang:'ar', id:'docs_forms_102', value:'تُعرض الخيارات التي تحمل <code>selected</code> كوسوم عند التحميل. بالنسبة إلى WebEOC، تقوم الدالة المساعدة <code>repopulateMultiSelect</code> على مستوى الصفحة بتقسيم السلسلة المحفوظة المفصولة بفواصل، وتعيين القيمة الأصلية، ثم استدعاء <code>.sync()</code>.' },
{ lang:'en', id:'docs_forms_103', value:'<span class="material-symbols-outlined">new_label</span>Creatable / tags' }, { lang:'ar', id:'docs_forms_103', value:'<span class="material-symbols-outlined">new_label</span>قابل للإنشاء / وسوم' },
{ lang:'en', id:'docs_forms_104', value:'<code>data-create="true"</code> lets the user type a new value and add it with Enter or Tab. The factory also enables <code>createOnBlur</code>, so tabbing away commits the typed entry automatically. Works on both single and multi selects; multi is the common "tags" pattern.' }, { lang:'ar', id:'docs_forms_104', value:'تتيح <code>data-create="true"</code> للمستخدم كتابة قيمة جديدة وإضافتها بمفتاح Enter أو Tab. كما يُفعّل المصنع <code>createOnBlur</code>، بحيث يؤدي الانتقال بمفتاح Tab إلى تثبيت الإدخال المكتوب تلقائيًا. تعمل مع الاختيار المفرد والمتعدد على حد سواء؛ والاختيار المتعدد هو نمط "الوسوم" الشائع.' },
{ lang:'en', id:'docs_forms_105', value:'Creatable multi (tags)' }, { lang:'ar', id:'docs_forms_105', value:'متعدد قابل للإنشاء (وسوم)' },
{ lang:'en', id:'docs_forms_106', value:'Creatable single' }, { lang:'ar', id:'docs_forms_106', value:'مفرد قابل للإنشاء' },
{ lang:'en', id:'docs_forms_107', value:'The same <code>data-create</code> flag on a single select lets the user commit one free-text value that is not in the option list.' }, { lang:'ar', id:'docs_forms_107', value:'تتيح راية <code>data-create</code> نفسها على اختيار مفرد للمستخدم تثبيت قيمة نصية حرة واحدة غير موجودة في قائمة الخيارات.' },
{ lang:'en', id:'docs_forms_108', value:'<span class="material-symbols-outlined">cloud_download</span>Remote / autocomplete (data-src="api")' }, { lang:'ar', id:'docs_forms_108', value:'<span class="material-symbols-outlined">cloud_download</span>عن بُعد / إكمال تلقائي (data-src="api")' },
{ lang:'en', id:'docs_forms_109', value:'<code>data-src="api"</code> turns the control into a remote autocomplete. The factory fetches <code>data-url?&lt;data-url-param&gt;=&lt;query&gt;</code> and maps each row through <code>data-value-field</code> / <code>data-label-field</code>. <code>data-preload="true"</code> loads once immediately; <code>data-preload="focus"</code> loads on first focus; omit to load only on keystroke. Placeholder injection and force-clear are skipped for API selects (options arrive asynchronously).' }, { lang:'ar', id:'docs_forms_109', value:'<code>data-src="api"</code> تحوّل عنصر التحكم إلى إكمال تلقائي عن بُعد. يجلب المصنع <code>data-url?&lt;data-url-param&gt;=&lt;query&gt;</code> ويربط كل صف عبر <code>data-value-field</code> / <code>data-label-field</code>. تُحمّل <code>data-preload="true"</code> مرة واحدة فورًا؛ وتُحمّل <code>data-preload="focus"</code> عند أول تركيز؛ واحذفها للتحميل عند الكتابة فقط. يُتخطى إدراج العنصر النائب والمسح القسري لقوائم اختيار الـ API (تصل الخيارات بشكل غير متزامن).' },
{ lang:'en', id:'docs_forms_110', value:'Live endpoint required' }, { lang:'ar', id:'docs_forms_110', value:'مطلوب نقطة نهاية نشطة' },
{ lang:'en', id:'docs_forms_111', value:'This demo points at a placeholder <code>data-url</code>. With no reachable endpoint the control renders and searches, but returns no rows. Point <code>data-url</code> at a real JSON endpoint that echoes an array of <code>{ id, text }</code> objects to see results.' }, { lang:'ar', id:'docs_forms_111', value:'يشير هذا العرض التوضيحي إلى <code>data-url</code> نائبة. بدون نقطة نهاية يمكن الوصول إليها، يُعرض عنصر التحكم ويبحث، لكنه لا يُرجع أي صفوف. وجّه <code>data-url</code> إلى نقطة نهاية JSON حقيقية تعيد مصفوفة من كائنات <code>{ id, text }</code> لرؤية النتائج.' },
{ lang:'en', id:'docs_forms_112', value:'Remote search — load on keystroke' }, { lang:'ar', id:'docs_forms_112', value:'بحث عن بُعد: التحميل عند الكتابة' },
{ lang:'en', id:'docs_forms_113', value:'Remote — preload on focus' }, { lang:'ar', id:'docs_forms_113', value:'عن بُعد: التحميل المسبق عند التركيز' },
{ lang:'en', id:'docs_forms_114', value:'<code>data-preload="focus"</code> fetches the initial result set the first time the control is focused, so an empty query already shows options.' }, { lang:'ar', id:'docs_forms_114', value:'تجلب <code>data-preload="focus"</code> مجموعة النتائج الأولية عند تركيز عنصر التحكم لأول مرة، بحيث يعرض الاستعلام الفارغ خيارات بالفعل.' },
{ lang:'en', id:'docs_forms_115', value:'<span class="material-symbols-outlined">segment</span>Optgroups' }, { lang:'ar', id:'docs_forms_115', value:'<span class="material-symbols-outlined">segment</span>مجموعات الخيارات' },
{ lang:'en', id:'docs_forms_116', value:'Native <code>&lt;optgroup label="…"&gt;</code> elements are honored — Tom Select renders each group with a pinned <code>optgroup-header</code>. Works with single and multi. No extra attributes are needed.' }, { lang:'ar', id:'docs_forms_116', value:'تُحترَم عناصر <code>&lt;optgroup label="…"&gt;</code> الأصلية، فيعرض Tom Select كل مجموعة برأس <code>optgroup-header</code> مثبّت. تعمل مع الاختيار المفرد والمتعدد. لا حاجة إلى سمات إضافية.' },
{ lang:'en', id:'docs_forms_117', value:'<span class="material-symbols-outlined">palette</span>Custom option / item renderers' }, { lang:'ar', id:'docs_forms_117', value:'<span class="material-symbols-outlined">palette</span>أدوات عرض مخصصة للخيار / العنصر' },
{ lang:'en', id:'docs_forms_118', value:'Register a render template with <code>TomSelectFactory.registerTemplate(name, def)</code> <strong>before</strong> <code>init()</code>, then point a select at it with <code>data-render="name"</code>. Tom Select copies every <code>data-*</code> attribute from each <code>&lt;option&gt;</code> into the option data object, so the template can render rich rows. Supported keys: <code>option</code>, <code>item</code>, <code>option_create</code>, <code>no_results</code>, <code>not_loading</code>, <code>optgroup</code>, <code>optgroup_header</code>, <code>loading</code>, <code>dropdown</code>. The agency CSS ships <code>.ts-person-option</code> / <code>.ts-person-name</code> / <code>.ts-person-meta</code> for this pattern.' }, { lang:'ar', id:'docs_forms_118', value:'سجّل قالب عرض باستخدام <code>TomSelectFactory.registerTemplate(name, def)</code> <strong>قبل</strong> <code>init()</code>، ثم وجّه قائمة اختيار إليه عبر <code>data-render="name"</code>. ينسخ Tom Select كل سمة <code>data-*</code> من كل <code>&lt;option&gt;</code> إلى كائن بيانات الخيار، بحيث يستطيع القالب عرض صفوف غنية. المفاتيح المدعومة: <code>option</code> و<code>item</code> و<code>option_create</code> و<code>no_results</code> و<code>not_loading</code> و<code>optgroup</code> و<code>optgroup_header</code> و<code>loading</code> و<code>dropdown</code>. يوفّر CSS الوكالة <code>.ts-person-option</code> / <code>.ts-person-name</code> / <code>.ts-person-meta</code> لهذا النمط.' },
{ lang:'en', id:'docs_forms_119', value:'<span class="material-symbols-outlined">account_tree</span>Dependent / cascading selects' }, { lang:'ar', id:'docs_forms_119', value:'<span class="material-symbols-outlined">account_tree</span>قوائم الاختيار التابعة / المتسلسلة' },
{ lang:'en', id:'docs_forms_120', value:'Two wiring styles. In WebEOC the board engine swaps the child\'s <code>&lt;option&gt;</code> list when the parent changes — mark the child with <code>data-parent="ParentName"</code> (or <code>parent="…"</code>) and the factory rebuilds the child from the currently visible native options. Outside WebEOC, wire <code>data-on-change="GlobalFn"</code> on the parent to a function that rebuilds the child options and calls <code>TomSelectFactory.get(\'child\').clearOptions()</code> then <code>.sync()</code>. The live demo below uses the <code>data-on-change</code> style.' }, { lang:'ar', id:'docs_forms_120', value:'أسلوبان للربط. في WebEOC، يبدّل محرك اللوحة قائمة <code>&lt;option&gt;</code> الخاصة بالعنصر التابع عند تغيّر العنصر الأصل، فعلّم العنصر التابع بـ <code>data-parent="ParentName"</code> (أو <code>parent="…"</code>) ويعيد المصنع بناء العنصر التابع من الخيارات الأصلية المرئية حاليًا. خارج WebEOC، اربط <code>data-on-change="GlobalFn"</code> على العنصر الأصل بدالة تعيد بناء خيارات العنصر التابع وتستدعي <code>TomSelectFactory.get(\'child\').clearOptions()</code> ثم <code>.sync()</code>. يستخدم العرض التوضيحي الحي أدناه أسلوب <code>data-on-change</code>.' },
{ lang:'en', id:'docs_forms_121', value:'<span class="material-symbols-outlined">vertical_split</span>Dropdown slots (header / footer)' }, { lang:'ar', id:'docs_forms_121', value:'<span class="material-symbols-outlined">vertical_split</span>فتحات القائمة المنسدلة (الرأس / التذييل)' },
{ lang:'en', id:'docs_forms_122', value:'Mount arbitrary markup as a pinned header and/or footer inside the dropdown. Stage a hidden <code>&lt;div class="ts-dropdown-slot"&gt;</code> in the page and point the select at it with <code>data-dropdown-header="#id"</code> / <code>data-dropdown-footer="#id"</code>. The factory <em>moves</em> the node (not a clone, so ids stay unique) into the dropdown and shields it from Tom Select\'s select/close handlers, so controls inside the slot stay interactive. Pair with <code>data-stay-open="true"</code> so picking an option does not close the dropdown while the user is also using the slot controls. Slots are also mountable manually via <code>TomSelectFactory.mountDropdownSlots(ts, el)</code>.' }, { lang:'ar', id:'docs_forms_122', value:'ثبّت أي ترميز كرأس و/أو تذييل مثبّت داخل القائمة المنسدلة. جهّز <code>&lt;div class="ts-dropdown-slot"&gt;</code> مخفيًا في الصفحة ووجّه قائمة الاختيار إليه عبر <code>data-dropdown-header="#id"</code> / <code>data-dropdown-footer="#id"</code>. <em>ينقل</em> المصنع العقدة (وليس نسخة عنها، لذا تبقى المعرّفات فريدة) إلى القائمة المنسدلة ويحميها من معالِجات الاختيار/الإغلاق في Tom Select، بحيث تبقى عناصر التحكم داخل الفتحة تفاعلية. اقرنها بـ <code>data-stay-open="true"</code> بحيث لا يؤدي اختيار خيار إلى إغلاق القائمة المنسدلة بينما يستخدم المستخدم عناصر تحكم الفتحة أيضًا. يمكن أيضًا تثبيت الفتحات يدويًا عبر <code>TomSelectFactory.mountDropdownSlots(ts, el)</code>.' },
{ lang:'en', id:'docs_forms_123', value:'Mount once' }, { lang:'ar', id:'docs_forms_123', value:'التثبيت مرة واحدة' },
{ lang:'en', id:'docs_forms_124', value:'The slot node is consumed into the dropdown, so a select that is destroyed and re-initialized loses its slot. Mount already-initialized controls.' }, { lang:'ar', id:'docs_forms_124', value:'تُستهلك عقدة الفتحة داخل القائمة المنسدلة، لذا تفقد قائمة الاختيار التي يتم إتلافها وإعادة تهيئتها فتحتها. ثبّت عناصر التحكم المهيّأة بالفعل.' },
{ lang:'en', id:'docs_forms_125', value:'Header slot + footer slot (stay-open)' }, { lang:'ar', id:'docs_forms_125', value:'فتحة الرأس + فتحة التذييل (البقاء مفتوحًا)' },
{ lang:'en', id:'docs_forms_126', value:'<span class="material-symbols-outlined">bolt</span>Change callback (data-on-change)' }, { lang:'ar', id:'docs_forms_126', value:'<span class="material-symbols-outlined">bolt</span>رد نداء التغيير (data-on-change)' },
{ lang:'en', id:'docs_forms_127', value:'<code>data-on-change="GlobalFn"</code> calls <code>window.GlobalFn(value, key, instance)</code> on every change — the value, the instance key, and the live Tom Select instance. Use it to drive dependent UI without touching the factory internals.' }, { lang:'ar', id:'docs_forms_127', value:'تستدعي <code>data-on-change="GlobalFn"</code> الدالة <code>window.GlobalFn(value, key, instance)</code> عند كل تغيير: القيمة، ومفتاح النسخة، ونسخة Tom Select النشطة. استخدمها لتشغيل واجهة مستخدم تابعة دون المساس بالأجزاء الداخلية للمصنع.' },
{ lang:'en', id:'docs_forms_128', value:'<span class="material-symbols-outlined">open_in_full</span>Dropdown parent (data-dropdown-parent)' }, { lang:'ar', id:'docs_forms_128', value:'<span class="material-symbols-outlined">open_in_full</span>العنصر الأصل للقائمة المنسدلة (data-dropdown-parent)' },
{ lang:'en', id:'docs_forms_129', value:'The dropdown teleports to <code>&lt;body&gt;</code> by default so it is never clipped by an <code>overflow:hidden</code> ancestor. Override the mount target with <code>data-dropdown-parent="&lt;selector&gt;"</code> when the dropdown must live inside a specific scroll container or modal.' }, { lang:'ar', id:'docs_forms_129', value:'تنتقل القائمة المنسدلة إلى <code>&lt;body&gt;</code> افتراضيًا بحيث لا يقتطعها أبدًا عنصر أصل يحمل <code>overflow:hidden</code>. تجاوز هدف التثبيت باستخدام <code>data-dropdown-parent="&lt;selector&gt;"</code> عندما يجب أن توجد القائمة المنسدلة داخل حاوية تمرير أو نافذة منبثقة محددة.' },
{ lang:'en', id:'docs_forms_130', value:'<span class="material-symbols-outlined">block</span>States — disabled &amp; readonly' }, { lang:'ar', id:'docs_forms_130', value:'<span class="material-symbols-outlined">block</span>الحالات: معطّل وللقراءة فقط' },
{ lang:'en', id:'docs_forms_131', value:'Disabled (native <code>disabled</code> attribute, or <code>TomSelectFactory.disable(\'key\')</code>) renders at reduced opacity, fully inert with no tab stop. Readonly (add <code>.is-readonly</code> to the <code>.ts-wrapper</code> after init) matches the form-control readonly style: dashed border, muted surface, text still selectable and copyable, and no dropdown opens.' }, { lang:'ar', id:'docs_forms_131', value:'يُعرض الوضع المعطّل (سمة <code>disabled</code> الأصلية، أو <code>TomSelectFactory.disable(\'key\')</code>) بعتامة منخفضة، خامل تمامًا دون نقطة توقف للتنقل بمفتاح Tab. أما الوضع للقراءة فقط (أضف <code>.is-readonly</code> إلى <code>.ts-wrapper</code> بعد التهيئة) فيطابق نمط عنصر التحكم للقراءة فقط: حدود متقطعة، وسطح باهت، ونص يبقى قابلًا للتحديد والنسخ، ولا تُفتح أي قائمة منسدلة.' }