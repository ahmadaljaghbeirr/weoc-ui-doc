# The build/ package folder is TRACKED (source of truth). Only its deps aren't.
node_modules/
*.log

# Isolated worktree workspaces (e.g. weoc-ui-angular-port)
.worktrees/

# Wrangler local state and development secrets (not the deploy configuration).
.wrangler/
.dev.vars*
!.dev.vars.example
.env*
!.env.example
.claude/worktrees/
